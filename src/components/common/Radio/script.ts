import { defineComponent } from '@vue/composition-api'

export default defineComponent({
    name: 'Radio',
    props: {
        label: {
            type: String
        }
    },
    setup() {
        
    },
})